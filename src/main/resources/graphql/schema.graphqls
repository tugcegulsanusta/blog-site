type User{
    id: ID!
    username: String!
    email: String!
    password: String!
}
type Query {
    findById(id: ID): User
}
type Mutation {
    createUser(username: String!, email: String!): User
}
